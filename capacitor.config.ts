import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile-app-chatting-socket.io',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
